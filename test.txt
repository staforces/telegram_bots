	b.Handle("/hello", func(m *tb.Message) {
		b.Send(m.Sender, "Приветствую тебя друг мой! Нужно больше /gold:)")
	})

	//buttons
	inlineBtn1 := tb.InlineButton{
		Unique: "wowtoken",
		Text:   "Курс WoW Token 🌚",
	}

	inlineBtn2 := tb.InlineButton{
		Unique: "dollar",
		Text:   "Курс Доллара 🌞",
	}
	inlineBtn3 := tb.InlineButton{
		Unique: "bitcoin",
		Text:   "Курс Bitcoin 🌚",
	}

	inlineBtn4 := tb.InlineButton{
		Unique: "tenge",
		Text:   "Курс Тенге 🌞",
	}
	b.Handle(&inlineBtn1, func(c *tb.Callback) {
		// Required for proper work
		b.Respond(c, &tb.CallbackResponse{
			ShowAlert: false,
		})
		// Send messages here
		b.Send(c.Sender, "Wow token says 'Hi'!")
	})

	b.Handle(&inlineBtn2, func(c *tb.Callback) {
		b.Respond(c, &tb.CallbackResponse{
			ShowAlert: false,
		})
		b.Send(c.Sender, "Доллар says 'Hi'!")
	})
	b.Handle(&inlineBtn3, func(c *tb.Callback) {
		// Required for proper work
		b.Respond(c, &tb.CallbackResponse{
			ShowAlert: false,
		})
		// Send messages here
		b.Send(c.Sender, "Биткоин says 'Hi'!")
	})

	b.Handle(&inlineBtn4, func(c *tb.Callback) {
		b.Respond(c, &tb.CallbackResponse{
			ShowAlert: false,
		})
		b.Send(c.Sender, "Тенге says 'Hi'!")
	})
	inlineKeys := [][]tb.InlineButton{
		[]tb.InlineButton{inlineBtn1, inlineBtn2,inlineBtn3,inlineBtn4},
	}
	b.Handle("/gold", func(m *tb.Message) {
		b.Send(
			m.Sender,
			"Выбери нужное!",
			&tb.ReplyMarkup{InlineKeyboard: inlineKeys})
	})
	b.Start()
